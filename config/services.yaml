# yaml-language-server: $schema=../vendor/symfony/dependency-injection/Loader/schema/services.schema.json
parameters:
    images_directory: '%kernel.project_dir%/public/uploads/hebergements'
    public_base_url: '%env(DEFAULT_URI)%'
    blog_translation_provider_url: '%env(default::BLOG_TRANSLATION_PROVIDER_URL)%'
    blog_translation_provider_api_key: '%env(default::BLOG_TRANSLATION_PROVIDER_API_KEY)%'
    blog_bad_words:
        - merde
        - putain
        - con
        - connard
        - connasse
        - salope
        - salaud
        - idiot
        - imbecile
        - debile
        - stupid
        - idiot
        - dumb
        - hell
        - damn
        - shit
        - bullshit
        - fuck
        - asshole
        - bastard

services:
    _defaults:
        autowire: true
        autoconfigure: true

    App\:
        resource: '../src/'

    App\Service\CommentModerationService:
        arguments:
            $blockedTerms: '%blog_bad_words%'

    App\Service\BlogTranslationService:
        arguments:
            $providerUrl: '%blog_translation_provider_url%'
            $providerApiKey: '%blog_translation_provider_api_key%'

    App\Service\NotificationService:
        arguments:
            $fcmProjectId: '%env(FCM_PROJECT_ID)%'
