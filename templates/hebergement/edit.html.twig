{% extends 'admin/base_admin.html.twig' %}
{% block title %}Modifier Hébergement{% endblock %}
{% block admin_content %}

    <div class="page-header">
        <h1>Modifier Hébergement #{{ hebergement.id }}</h1>
        <p><a href="{{ path('app_hebergement_index') }}" style="color:var(--accent-primary);">← Retour à la liste</a></p>
    </div>

    <div class="card" style="max-width:700px;">
        {{ include('hebergement/_form.html.twig', {'button_label': 'Mettre à jour'}) }}
    </div>

    <div style="margin-top:16px; max-width:700px;">
        {{ include('hebergement/_delete_form.html.twig') }}
    </div>

    <footer class="copyright">Copyright &copy; 2026 Voyage</footer>

{% endblock %}
